Added ``toTokenBaseUnits`` operator for condition variable operations (takes ``decimals`` as value to convert token amounts to base units, e.g., ``100.5`` USDC with ``decimals=6`` becomes ``100500000``), useful for ERC-20 token comparisons, and increased condition limits (``SequentialCondition.MAX_NUM_CONDITIONS`` to 20, ``MAX_MULTI_CONDITION_NESTED_LEVEL`` to 4).
